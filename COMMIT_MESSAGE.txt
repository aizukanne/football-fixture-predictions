feat(api): include actual scores in league search; fix best_bet update error

1. Added actual match scores to league search results
   - Modified src/services/data_formatter.py to include 'goals' field
   - Previously only available in single fixture queries
   - Enables comparison of predictions vs actual outcomes in bulk queries

2. Fixed DynamoDB ValidationException in best_bet updates
   - Modified src/data/database_client.py update_fixture_best_bet()
   - Root cause: timestamp retrieved as Decimal but needs int for key matching
   - Error: "The provided key element does not match the schema"
   - Solution: Convert timestamp to int before using in update_item Key

Changes:
- src/services/data_formatter.py: Added goals field to _format_single_fixture()
- src/data/database_client.py: Convert timestamp Decimal to int (line 481)

Benefits:
- Best bets now save correctly to game_fixtures table
- League queries show actual scores alongside predictions for finished matches
- Maintains backward compatibility (goals field only present if data exists)

Response format example:
{
  "fixture_id": 123,
  "home": { "predicted_goals": 1.8 },
  "away": { "predicted_goals": 1.2 },
  "goals": { "home": 2, "away": 1 },  // NEW - only if match finished
  "best_bet": [...]                    // Now saves correctly
}
